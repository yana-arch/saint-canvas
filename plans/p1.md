# 🎨 Ý Tưởng App Chỉnh Sửa Hình Ảnh Thánh - "SaintCanvas"

## 📋 Tổng Quan Dự Án

**Tên App:** SaintCanvas (hoặc "Thánh Hình" cho phiên bản tiếng Việt)

**Mục đích:** Ứng dụng web cho phép người dùng chỉnh sửa hình ảnh với các yếu tố Công giáo - trang phục thánh, nền nhà thờ, thêm các Thánh và biểu tượng tôn giáo.

---

## 🏗️ Kiến Trúc Tổng Thể

```
┌─────────────────────────────────────────────────────────────────┐
│                        FRONTEND (React)                         │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐ │
│  │  Upload  │  │  Editor  │  │ Library  │  │  Preview/Export  │ │
│  │  Module  │  │  Canvas  │  │  Panel   │  │     Module       │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘ │
├─────────────────────────────────────────────────────────────────┤
│                    State Management (Zustand/Redux)             │
├─────────────────────────────────────────────────────────────────┤
│                         API Layer (Axios/Fetch)                 │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      BACKEND API (Optional)                      │
│         (AI Image Processing / Asset Management)                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📁 Cấu Trúc Thư Mục

```
src/
├── components/
│   ├── Layout/
│   │   ├── Header.jsx
│   │   ├── Sidebar.jsx
│   │   └── Footer.jsx
│   │
│   ├── Upload/
│   │   ├── ImageUploader.jsx
│   │   ├── DragDropZone.jsx
│   │   └── ImagePreview.jsx
│   │
│   ├── Editor/
│   │   ├── EditorCanvas.jsx
│   │   ├── ToolBar.jsx
│   │   ├── LayerPanel.jsx
│   │   ├── TransformControls.jsx
│   │   └── HistoryPanel.jsx
│   │
│   ├── Library/
│   │   ├── AssetLibrary.jsx
│   │   ├── CategoryTabs.jsx
│   │   │
│   │   ├── Saints/
│   │   │   ├── SaintGallery.jsx
│   │   │   ├── SaintCard.jsx
│   │   │   └── SaintSearch.jsx
│   │   │
│   │   ├── Vestments/
│   │   │   ├── VestmentGallery.jsx
│   │   │   ├── VestmentCard.jsx
│   │   │   └── ColorPicker.jsx
│   │   │
│   │   ├── Backgrounds/
│   │   │   ├── BackgroundGallery.jsx
│   │   │   └── BackgroundCard.jsx
│   │   │
│   │   └── Objects/
│   │       ├── ObjectGallery.jsx
│   │       └── ObjectCard.jsx
│   │
│   ├── Effects/
│   │   ├── HaloEffect.jsx
│   │   ├── LightRays.jsx
│   │   └── GlowEffect.jsx
│   │
│   └── Export/
│       ├── ExportModal.jsx
│       ├── QualitySelector.jsx
│       └── WatermarkOptions.jsx
│
├── pages/
│   ├── HomePage.jsx
│   ├── EditorPage.jsx
│   ├── GalleryPage.jsx
│   └── TutorialPage.jsx
│
├── hooks/
│   ├── useCanvas.js
│   ├── useImageUpload.js
│   ├── useHistory.js
│   ├── useLayers.js
│   └── useExport.js
│
├── store/
│   ├── editorStore.js
│   ├── assetStore.js
│   └── userStore.js
│
├── services/
│   ├── imageService.js
│   ├── assetService.js
│   └── aiService.js
│
├── utils/
│   ├── imageUtils.js
│   ├── canvasUtils.js
│   └── constants.js
│
├── assets/
│   ├── saints/
│   ├── vestments/
│   ├── backgrounds/
│   ├── objects/
│   └── icons/
│
└── styles/
    ├── global.css
    ├── editor.css
    └── components/
```

---

## 🎯 Tính Năng Chi Tiết

### 1️⃣ **Module Upload Hình Ảnh**

```jsx
// components/Upload/ImageUploader.jsx
import React, { useCallback } from 'react';
import { useDropzone } from 'react-dropzone';
import { useEditorStore } from '../../store/editorStore';

const ImageUploader = () => {
  const setBaseImage = useEditorStore((state) => state.setBaseImage);
  
  const onDrop = useCallback((acceptedFiles) => {
    const file = acceptedFiles[0];
    const reader = new FileReader();
    
    reader.onload = (e) => {
      setBaseImage({
        src: e.target.result,
        name: file.name,
        size: file.size,
        type: file.type
      });
    };
    
    reader.readAsDataURL(file);
  }, [setBaseImage]);

  const { getRootProps, getInputProps, isDragActive } = useDropzone({
    onDrop,
    accept: {
      'image/*': ['.jpeg', '.jpg', '.png', '.webp']
    },
    maxSize: 10485760, // 10MB
    multiple: false
  });

  return (
    <div 
      {...getRootProps()} 
      className={`upload-zone ${isDragActive ? 'active' : ''}`}
    >
      <input {...getInputProps()} />
      <div className="upload-content">
        <i className="icon-upload" />
        <h3>Tải Hình Ảnh Lên</h3>
        <p>Kéo thả hoặc click để chọn hình</p>
        <span className="supported-formats">
          Hỗ trợ: JPG, PNG, WebP (Tối đa 10MB)
        </span>
      </div>
    </div>
  );
};

export default ImageUploader;
```

### 2️⃣ **Editor Canvas - Trung Tâm Chỉnh Sửa**

```jsx
// components/Editor/EditorCanvas.jsx
import React, { useRef, useEffect, useState } from 'react';
import { Stage, Layer, Image, Transformer } from 'react-konva';
import useImage from 'use-image';
import { useEditorStore } from '../../store/editorStore';

const EditorCanvas = () => {
  const stageRef = useRef(null);
  const { baseImage, layers, selectedLayer, updateLayer, setSelectedLayer } = useEditorStore();
  
  const [dimensions, setDimensions] = useState({
    width: 800,
    height: 600
  });

  // Layer Component
  const LayerItem = ({ layer, isSelected, onSelect }) => {
    const [image] = useImage(layer.src);
    const shapeRef = useRef();
    const transformerRef = useRef();

    useEffect(() => {
      if (isSelected && transformerRef.current && shapeRef.current) {
        transformerRef.current.nodes([shapeRef.current]);
        transformerRef.current.getLayer().batchDraw();
      }
    }, [isSelected]);

    return (
      <>
        <Image
          ref={shapeRef}
          image={image}
          x={layer.x}
          y={layer.y}
          width={layer.width}
          height={layer.height}
          rotation={layer.rotation}
          scaleX={layer.scaleX}
          scaleY={layer.scaleY}
          opacity={layer.opacity}
          draggable
          onClick={() => onSelect(layer.id)}
          onTap={() => onSelect(layer.id)}
          onDragEnd={(e) => {
            updateLayer(layer.id, {
              x: e.target.x(),
              y: e.target.y()
            });
          }}
          onTransformEnd={(e) => {
            const node = shapeRef.current;
            updateLayer(layer.id, {
              x: node.x(),
              y: node.y(),
              rotation: node.rotation(),
              scaleX: node.scaleX(),
              scaleY: node.scaleY()
            });
          }}
        />
        {isSelected && (
          <Transformer
            ref={transformerRef}
            boundBoxFunc={(oldBox, newBox) => {
              if (newBox.width < 20 || newBox.height < 20) {
                return oldBox;
              }
              return newBox;
            }}
          />
        )}
      </>
    );
  };

  return (
    <div className="editor-canvas-wrapper">
      <Stage
        ref={stageRef}
        width={dimensions.width}
        height={dimensions.height}
        className="editor-stage"
      >
        {/* Background Layer */}
        <Layer>
          {baseImage && <BaseImageComponent src={baseImage.src} />}
        </Layer>
        
        {/* Editable Layers */}
        <Layer>
          {layers.map((layer) => (
            <LayerItem
              key={layer.id}
              layer={layer}
              isSelected={selectedLayer === layer.id}
              onSelect={setSelectedLayer}
            />
          ))}
        </Layer>
      </Stage>
      
      {/* Zoom Controls */}
      <ZoomControls stageRef={stageRef} />
    </div>
  );
};

export default EditorCanvas;
```

### 3️⃣ **Thư Viện Tài Nguyên Công Giáo**

```jsx
// components/Library/AssetLibrary.jsx
import React, { useState, useMemo } from 'react';
import { Tab, Tabs, TabList, TabPanel } from 'react-tabs';
import SaintGallery from './Saints/SaintGallery';
import VestmentGallery from './Vestments/VestmentGallery';
import BackgroundGallery from './Backgrounds/BackgroundGallery';
import ObjectGallery from './Objects/ObjectGallery';

const CATEGORIES = [
  { id: 'saints', label: 'Các Thánh', icon: '✝️' },
  { id: 'vestments', label: 'Trang Phục', icon: '👗' },
  { id: 'backgrounds', label: 'Phông Nền', icon: '🏛️' },
  { id: 'objects', label: 'Đối Tượng', icon: '🕯️' }
];

const AssetLibrary = () => {
  const [activeTab, setActiveTab] = useState(0);
  const [searchQuery, setSearchQuery] = useState('');

  return (
    <div className="asset-library">
      <div className="library-header">
        <h2>Thư Viện Tài Nguyên</h2>
        <input
          type="text"
          placeholder="Tìm kiếm..."
          value={searchQuery}
          onChange={(e) => setSearchQuery(e.target.value)}
          className="search-input"
        />
      </div>

      <Tabs selectedIndex={activeTab} onSelect={setActiveTab}>
        <TabList className="category-tabs">
          {CATEGORIES.map((cat) => (
            <Tab key={cat.id} className="category-tab">
              <span className="tab-icon">{cat.icon}</span>
              <span className="tab-label">{cat.label}</span>
            </Tab>
          ))}
        </TabList>

        <TabPanel>
          <SaintGallery searchQuery={searchQuery} />
        </TabPanel>
        <TabPanel>
          <VestmentGallery searchQuery={searchQuery} />
        </TabPanel>
        <TabPanel>
          <BackgroundGallery searchQuery={searchQuery} />
        </TabPanel>
        <TabPanel>
          <ObjectGallery searchQuery={searchQuery} />
        </TabPanel>
      </Tabs>
    </div>
  );
};

export default AssetLibrary;
```

### 4️⃣ **Data Structure - Tài Nguyên Công Giáo**

```javascript
// utils/constants.js

export const SAINTS = [
  {
    id: 'saint-mary',
    name: 'Đức Mẹ Maria',
    nameEn: 'Virgin Mary',
    category: 'Holy Family',
    feastDay: '15/08',
    description: 'Mẹ Thiên Chúa',
    images: {
      portrait: '/assets/saints/mary-portrait.png',
      fullBody: '/assets/saints/mary-full.png',
      icon: '/assets/saints/mary-icon.png'
    },
    attributes: ['blue_mantle', 'crown', 'stars', 'roses']
  },
  {
    id: 'saint-joseph',
    name: 'Thánh Giuse',
    nameEn: 'Saint Joseph',
    category: 'Holy Family',
    feastDay: '19/03',
    description: 'Bổn mạng Giáo Hội Hoàn Vũ',
    images: {
      portrait: '/assets/saints/joseph-portrait.png',
      fullBody: '/assets/saints/joseph-full.png',
      icon: '/assets/saints/joseph-icon.png'
    },
    attributes: ['lily', 'carpenter_tools', 'infant_jesus']
  },
  {
    id: 'saint-peter',
    name: 'Thánh Phêrô Tông Đồ',
    nameEn: 'Saint Peter',
    category: 'Apostles',
    feastDay: '29/06',
    description: 'Giáo Hoàng tiên khởi',
    images: {
      portrait: '/assets/saints/peter-portrait.png',
      fullBody: '/assets/saints/peter-full.png',
      icon: '/assets/saints/peter-icon.png'
    },
    attributes: ['keys', 'papal_tiara', 'inverted_cross']
  },
  {
    id: 'saint-paul',
    name: 'Thánh Phaolô Tông Đồ',
    nameEn: 'Saint Paul',
    category: 'Apostles',
    feastDay: '29/06',
    description: 'Tông đồ dân ngoại',
    images: {
      portrait: '/assets/saints/paul-portrait.png',
      fullBody: '/assets/saints/paul-full.png'
    },
    attributes: ['sword', 'book', 'scroll']
  },
  {
    id: 'saint-francis-assisi',
    name: 'Thánh Phanxicô Assisi',
    nameEn: 'Saint Francis of Assisi',
    category: 'Saints',
    feastDay: '04/10',
    description: 'Sáng lập dòng Phanxicô',
    images: {
      portrait: '/assets/saints/francis-portrait.png',
      fullBody: '/assets/saints/francis-full.png'
    },
    attributes: ['brown_habit', 'stigmata', 'animals', 'crucifix']
  },
  // ... Thêm nhiều Thánh khác
];

export const VESTMENTS = [
  {
    id: 'alb',
    name: 'Áo Alba',
    nameEn: 'Alb',
    category: 'base',
    description: 'Áo trắng dài - trang phục cơ bản',
    colors: ['white'],
    images: {
      default: '/assets/vestments/alb.png'
    }
  },
  {
    id: 'chasuble',
    name: 'Áo Lễ (Casula)',
    nameEn: 'Chasuble',
    category: 'outer',
    description: 'Áo lễ của Linh mục',
    colors: ['white', 'red', 'green', 'violet', 'rose', 'black', 'gold'],
    images: {
      white: '/assets/vestments/chasuble-white.png',
      red: '/assets/vestments/chasuble-red.png',
      green: '/assets/vestments/chasuble-green.png',
      violet: '/assets/vestments/chasuble-violet.png',
      rose: '/assets/vestments/chasuble-rose.png',
      black: '/assets/vestments/chasuble-black.png',
      gold: '/assets/vestments/chasuble-gold.png'
    }
  },
  {
    id: 'stole',
    name: 'Dây Stola',
    nameEn: 'Stole',
    category: 'accessory',
    description: 'Dây đeo vai của chức thánh',
    colors: ['white', 'red', 'green', 'violet', 'rose', 'black'],
    images: {
      priest: '/assets/vestments/stole-priest.png',
      deacon: '/assets/vestments/stole-deacon.png'
    }
  },
  {
    id: 'dalmatic',
    name: 'Áo Dalmatic',
    nameEn: 'Dalmatic',
    category: 'outer',
    description: 'Áo lễ của Phó tế',
    colors: ['white', 'red', 'green', 'violet'],
    images: {
      default: '/assets/vestments/dalmatic.png'
    }
  },
  {
    id: 'cope',
    name: 'Áo Choàng',
    nameEn: 'Cope',
    category: 'outer',
    description: 'Áo choàng dùng trong các nghi thức',
    colors: ['white', 'red', 'violet', 'gold'],
    images: {
      default: '/assets/vestments/cope.png'
    }
  },
  {
    id: 'miter',
    name: 'Mũ Giám Mục',
    nameEn: 'Miter',
    category: 'headwear',
    description: 'Mũ phụng vụ của Giám mục',
    images: {
      simple: '/assets/vestments/miter-simple.png',
      precious: '/assets/vestments/miter-precious.png'
    }
  },
  {
    id: 'religious-habit',
    name: 'Áo Dòng Tu',
    nameEn: 'Religious Habit',
    category: 'religious_order',
    subcategories: [
      { id: 'franciscan', name: 'Dòng Phanxicô', color: 'brown' },
      { id: 'dominican', name: 'Dòng Đa Minh', color: 'black_white' },
      { id: 'benedictine', name: 'Dòng Biển Đức', color: 'black' },
      { id: 'carmelite', name: 'Dòng Cát Minh', color: 'brown_white' }
    ]
  }
];

export const BACKGROUNDS = [
  {
    id: 'bg-church-interior',
    name: 'Nội Thất Nhà Thờ',
    nameEn: 'Church Interior',
    category: 'church',
    subcategories: [
      { id: 'altar', name: 'Cung Thánh' },
      { id: 'nave', name: 'Gian Giữa' },
      { id: 'chapel', name: 'Nguyện Đường' }
    ],
    images: {
      altar: '/assets/backgrounds/altar.jpg',
      nave: '/assets/backgrounds/nave.jpg',
      chapel: '/assets/backgrounds/chapel.jpg'
    }
  },
  {
    id: 'bg-stained-glass',
    name: 'Kính Màu',
    nameEn: 'Stained Glass',
    category: 'decorative',
    images: {
      default: '/assets/backgrounds/stained-glass.png',
      blue: '/assets/backgrounds/stained-glass-blue.png',
      red: '/assets/backgrounds/stained-glass-red.png'
    }
  },
  {
    id: 'bg-heaven',
    name: 'Thiên Đàng',
    nameEn: 'Heaven',
    category: 'spiritual',
    images: {
      clouds: '/assets/backgrounds/heaven-clouds.jpg',
      golden: '/assets/backgrounds/heaven-golden.jpg',
      angels: '/assets/backgrounds/heaven-angels.jpg'
    }
  },
  {
    id: 'bg-vatican',
    name: 'Vatican',
    nameEn: 'Vatican',
    category: 'location',
    images: {
      stPeters: '/assets/backgrounds/vatican-st-peters.jpg',
      sistineChapel: '/assets/backgrounds/sistine-chapel.jpg',
      square: '/assets/backgrounds/vatican-square.jpg'
    }
  },
  {
    id: 'bg-holy-land',
    name: 'Đất Thánh',
    nameEn: 'Holy Land',
    category: 'location',
    images: {
      jerusalem: '/assets/backgrounds/jerusalem.jpg',
      bethlehem: '/assets/backgrounds/bethlehem.jpg',
      nazareth: '/assets/backgrounds/nazareth.jpg'
    }
  }
];

export const OBJECTS = [
  // Liturgical Items
  {
    id: 'crucifix',
    name: 'Thánh Giá Chịu Nạn',
    nameEn: 'Crucifix',
    category: 'liturgical',
    images: {
      wooden: '/assets/objects/crucifix-wooden.png',
      golden: '/assets/objects/crucifix-golden.png',
      sanDamiano: '/assets/objects/crucifix-san-damiano.png'
    }
  },
  {
    id: 'cross',
    name: 'Thánh Giá',
    nameEn: 'Cross',
    category: 'liturgical',
    images: {
      latin: '/assets/objects/cross-latin.png',
      celtic: '/assets/objects/cross-celtic.png',
      papal: '/assets/objects/cross-papal.png'
    }
  },
  {
    id: 'rosary',
    name: 'Chuỗi Mân Côi',
    nameEn: 'Rosary',
    category: 'devotional',
    images: {
      default: '/assets/objects/rosary.png',
      golden: '/assets/objects/rosary-golden.png'
    }
  },
  {
    id: 'chalice',
    name: 'Chén Thánh',
    nameEn: 'Chalice',
    category: 'liturgical',
    images: {
      golden: '/assets/objects/chalice-golden.png',
      silver: '/assets/objects/chalice-silver.png'
    }
  },
  {
    id: 'candles',
    name: 'Nến',
    nameEn: 'Candles',
    category: 'liturgical',
    images: {
      paschal: '/assets/objects/candle-paschal.png',
      votive: '/assets/objects/candles-votive.png',
      altar: '/assets/objects/candles-altar.png'
    }
  },
  {
    id: 'incense',
    name: 'Bình Hương',
    nameEn: 'Thurible/Censer',
    category: 'liturgical',
    images: {
      thurible: '/assets/objects/thurible.png',
      smoke: '/assets/objects/incense-smoke.png'
    }
  },
  {
    id: 'bible',
    name: 'Sách Thánh',
    nameEn: 'Bible/Lectionary',
    category: 'books',
    images: {
      bible: '/assets/objects/bible.png',
      lectionary: '/assets/objects/lectionary.png',
      missal: '/assets/objects/missal.png'
    }
  },
  
  // Marian Items
  {
    id: 'miraculous-medal',
    name: 'Mề Đai Lạ',
    nameEn: 'Miraculous Medal',
    category: 'marian',
    images: {
      default: '/assets/objects/miraculous-medal.png'
    }
  },
  {
    id: 'scapular',
    name: 'Áo Đức Bà',
    nameEn: 'Scapular',
    category: 'marian',
    images: {
      brown: '/assets/objects/scapular-brown.png',
      green: '/assets/objects/scapular-green.png'
    }
  },
  
  // Effects & Halos
  {
    id: 'halo',
    name: 'Vầng Hào Quang',
    nameEn: 'Halo',
    category: 'effects',
    images: {
      circular: '/assets/objects/halo-circle.png',
      triangular: '/assets/objects/halo-trinity.png',
      cruciform: '/assets/objects/halo-cross.png'
    }
  },
  {
    id: 'light-rays',
    name: 'Tia Sáng',
    nameEn: 'Divine Light Rays',
    category: 'effects',
    images: {
      default: '/assets/objects/light-rays.png',
      golden: '/assets/objects/light-rays-golden.png'
    }
  },
  {
    id: 'dove',
    name: 'Chim Bồ Câu (Chúa Thánh Thần)',
    nameEn: 'Holy Spirit Dove',
    category: 'symbols',
    images: {
      default: '/assets/objects/dove.png',
      rays: '/assets/objects/dove-rays.png'
    }
  },
  
  // Flowers & Nature
  {
    id: 'lily',
    name: 'Hoa Huệ',
    nameEn: 'Lily',
    category: 'nature',
    symbolism: 'Sự trinh khiết',
    images: {
      default: '/assets/objects/lily.png',
      bouquet: '/assets/objects/lily-bouquet.png'
    }
  },
  {
    id: 'rose',
    name: 'Hoa Hồng',
    nameEn: 'Rose',
    category: 'nature',
    symbolism: 'Đức Mẹ',
    images: {
      red: '/assets/objects/rose-red.png',
      white: '/assets/objects/rose-white.png',
      mystical: '/assets/objects/rose-mystical.png'
    }
  }
];

// Liturgical Colors Reference
export const LITURGICAL_COLORS = {
  white: {
    name: 'Trắng',
    occasions: ['Lễ Phục Sinh', 'Lễ Giáng Sinh', 'Lễ Các Thánh', 'Lễ Đức Mẹ'],
    meaning: 'Niềm vui, sự tinh khiết, chiến thắng'
  },
  red: {
    name: 'Đỏ',
    occasions: ['Lễ Hiện Xuống', 'Lễ Các Thánh Tử Đạo', 'Thứ Sáu Tuần Thánh'],
    meaning: 'Máu các Thánh Tử Đạo, Chúa Thánh Thần'
  },
  green: {
    name: 'Xanh Lá',
    occasions: ['Mùa Thường Niên'],
    meaning: 'Sự hy vọng, sự sống'
  },
  violet: {
    name: 'Tím',
    occasions: ['Mùa Vọng', 'Mùa Chay'],
    meaning: 'Sự sám hối, chuẩn bị'
  },
  rose: {
    name: 'Hồng',
    occasions: ['Chúa Nhật III Mùa Vọng (Gaudete)', 'Chúa Nhật IV Mùa Chay (Laetare)'],
    meaning: 'Niềm vui trong thời gian sám hối'
  },
  black: {
    name: 'Đen',
    occasions: ['Lễ Cầu Hồn', 'Thứ Sáu Tuần Thánh (tùy chọn)'],
    meaning: 'Tang chế, tưởng nhớ'
  },
  gold: {
    name: 'Vàng',
    occasions: ['Các lễ trọng (thay cho trắng)'],
    meaning: 'Sự vinh hiển, lễ trọng'
  }
};
```

### 5️⃣ **State Management (Zustand)**

```javascript
// store/editorStore.js
import { create } from 'zustand';
import { devtools, persist } from 'zustand/middleware';
import { v4 as uuidv4 } from 'uuid';

export const useEditorStore = create(
  devtools(
    persist(
      (set, get) => ({
        // Base Image
        baseImage: null,
        setBaseImage: (image) => set({ baseImage: image }),
        
        // Layers
        layers: [],
        selectedLayer: null,
        
        addLayer: (asset) => {
          const newLayer = {
            id: uuidv4(),
            type: asset.category,
            assetId: asset.id,
            name: asset.name,
            src: asset.images.default || Object.values(asset.images)[0],
            x: 100,
            y: 100,
            width: 200,
            height: 200,
            rotation: 0,
            scaleX: 1,
            scaleY: 1,
            opacity: 1,
            visible: true,
            locked: false,
            zIndex: get().layers.length
          };
          
          set((state) => ({
            layers: [...state.layers, newLayer],
            selectedLayer: newLayer.id
          }));
        },
        
        updateLayer: (layerId, updates) => {
          set((state) => ({
            layers: state.layers.map((layer) =>
              layer.id === layerId ? { ...layer, ...updates } : layer
            )
          }));
        },
        
        deleteLayer: (layerId) => {
          set((state) => ({
            layers: state.layers.filter((layer) => layer.id !== layerId),
            selectedLayer: state.selectedLayer === layerId ? null : state.selectedLayer
          }));
        },
        
        setSelectedLayer: (layerId) => set({ selectedLayer: layerId }),
        
        reorderLayers: (fromIndex, toIndex) => {
          set((state) => {
            const newLayers = [...state.layers];
            const [movedLayer] = newLayers.splice(fromIndex, 1);
            newLayers.splice(toIndex, 0, movedLayer);
            return { layers: newLayers };
          });
        },
        
        duplicateLayer: (layerId) => {
          const layer = get().layers.find((l) => l.id === layerId);
          if (layer) {
            const duplicated = {
              ...layer,
              id: uuidv4(),
              name: `${layer.name} (copy)`,
              x: layer.x + 20,
              y: layer.y + 20
            };
            set((state) => ({
              layers: [...state.layers, duplicated],
              selectedLayer: duplicated.id
            }));
          }
        },
        
        // History
        history: [],
        historyIndex: -1,
        
        saveToHistory: () => {
          const currentState = {
            layers: get().layers,
            baseImage: get().baseImage
          };
          
          set((state) => ({
            history: [...state.history.slice(0, state.historyIndex + 1), currentState],
            historyIndex: state.historyIndex + 1
          }));
        },
        
        undo: () => {
          const { historyIndex, history } = get();
          if (historyIndex > 0) {
            const prevState = history[historyIndex - 1];
            set({
              layers: prevState.layers,
              historyIndex: historyIndex - 1
            });
          }
        },
        
        redo: () => {
          const { historyIndex, history } = get();
          if (historyIndex < history.length - 1) {
            const nextState = history[historyIndex + 1];
            set({
              layers: nextState.layers,
              historyIndex: historyIndex + 1
            });
          }
        },
        
        // Canvas Settings
        canvasSettings: {
          width: 800,
          height: 600,
          zoom: 1,
          gridVisible: false
        },
        
        updateCanvasSettings: (settings) => {
          set((state) => ({
            canvasSettings: { ...state.canvasSettings, ...settings }
          }));
        },
        
        // Reset
        resetEditor: () => {
          set({
            baseImage: null,
            layers: [],
            selectedLayer: null,
            history: [],
            historyIndex: -1
          });
        }
      }),
      {
        name: 'saint-canvas-storage',
        partialize: (state) => ({
          canvasSettings: state.canvasSettings
        })
      }
    )
  )
);
```

### 6️⃣ **Toolbar Component**

```jsx
// components/Editor/ToolBar.jsx
import React from 'react';
import { useEditorStore } from '../../store/editorStore';

const ToolBar = () => {
  const {
    selectedLayer,
    layers,
    updateLayer,
    deleteLayer,
    duplicateLayer,
    undo,
    redo,
    historyIndex,
    history
  } = useEditorStore();

  const currentLayer = layers.find((l) => l.id === selectedLayer);

  return (
    <div className="toolbar">
      {/* History Controls */}
      <div className="toolbar-section">
        <button 
          onClick={undo} 
          disabled={historyIndex <= 0}
          title="Hoàn tác (Ctrl+Z)"
        >
          <i className="icon-undo" />
        </button>
        <button 
          onClick={redo}
          disabled={historyIndex >= history.length - 1}
          title="Làm lại (Ctrl+Y)"
        >
          <i className="icon-redo" />
        </button>
      </div>

      <div className="toolbar-divider" />

      {/* Layer Controls */}
      {currentLayer && (
        <div className="toolbar-section">
          <button 
            onClick={() => duplicateLayer(selectedLayer)}
            title="Nhân đôi"
          >
            <i className="icon-duplicate" />
          </button>
          <button 
            onClick={() => deleteLayer(selectedLayer)}
            title="Xóa (Delete)"
          >
            <i className="icon-delete" />
          </button>
          
          <div className="toolbar-divider" />
          
          {/* Opacity Control */}
          <div className="opacity-control">
            <label>Độ trong suốt:</label>
            <input
              type="range"
              min="0"
              max="1"
              step="0.1"
              value={currentLayer.opacity}
              onChange={(e) => updateLayer(selectedLayer, {
                opacity: parseFloat(e.target.value)
              })}
            />
            <span>{Math.round(currentLayer.opacity * 100)}%</span>
          </div>

          {/* Flip Controls */}
          <button
            onClick={() => updateLayer(selectedLayer, {
              scaleX: currentLayer.scaleX * -1
            })}
            title="Lật ngang"
          >
            <i className="icon-flip-horizontal" />
          </button>
          <button
            onClick={() => updateLayer(selectedLayer, {
              scaleY: currentLayer.scaleY * -1
            })}
            title="Lật dọc"
          >
            <i className="icon-flip-vertical" />
          </button>
        </div>
      )}
    </div>
  );
};

export default ToolBar;
```

### 7️⃣ **Layer Panel**

```jsx
// components/Editor/LayerPanel.jsx
import React from 'react';
import { DragDropContext, Droppable, Draggable } from 'react-beautiful-dnd';
import { useEditorStore } from '../../store/editorStore';

const LayerPanel = () => {
  const {
    layers,
    selectedLayer,
    setSelectedLayer,
    updateLayer,
    deleteLayer,
    reorderLayers
  } = useEditorStore();

  const handleDragEnd = (result) => {
    if (!result.destination) return;
    reorderLayers(result.source.index, result.destination.index);
  };

  return (
    <div className="layer-panel">
      <div className="panel-header">
        <h3>Các Lớp</h3>
        <span className="layer-count">{layers.length} lớp</span>
      </div>

      <DragDropContext onDragEnd={handleDragEnd}>
        <Droppable droppableId="layers">
          {(provided) => (
            <div
              {...provided.droppableProps}
              ref={provided.innerRef}
              className="layers-list"
            >
              {[...layers].reverse().map((layer, index) => (
                <Draggable
                  key={layer.id}
                  draggableId={layer.id}
                  index={index}
                >
                  {(provided, snapshot) => (
                    <div
                      ref={provided.innerRef}
                      {...provided.draggableProps}
                      {...provided.dragHandleProps}
                      className={`layer-item ${
                        selectedLayer === layer.id ? 'selected' : ''
                      } ${snapshot.isDragging ? 'dragging' : ''}`}
                      onClick={() => setSelectedLayer(layer.id)}
                    >
                      <div className="layer-thumbnail">
                        <img src={layer.src} alt={layer.name} />
                      </div>
                      
                      <div className="layer-info">
                        <span className="layer-name">{layer.name}</span>
                        <span className="layer-type">{layer.type}</span>
                      </div>

                      <div className="layer-controls">
                        <button
                          className={`visibility-btn ${layer.visible ? 'visible' : ''}`}
                          onClick={(e) => {
                            e.stopPropagation();
                            updateLayer(layer.id, { visible: !layer.visible });
                          }}
                        >
                          <i className={layer.visible ? 'icon-eye' : 'icon-eye-off'} />
                        </button>
                        
                        <button
                          className={`lock-btn ${layer.locked ? 'locked' : ''}`}
                          onClick={(e) => {
                            e.stopPropagation();
                            updateLayer(layer.id, { locked: !layer.locked });
                          }}
                        >
                          <i className={layer.locked ? 'icon-lock' : 'icon-unlock'} />
                        </button>
                      </div>
                    </div>
                  )}
                </Draggable>
              ))}
              {provided.placeholder}
            </div>
          )}
        </Droppable>
      </DragDropContext>
    </div>
  );
};

export default LayerPanel;
```

### 8️⃣ **Export Module**

```jsx
// components/Export/ExportModal.jsx
import React, { useState } from 'react';
import { useEditorStore } from '../../store/editorStore';

const ExportModal = ({ isOpen, onClose, stageRef }) => {
  const [settings, setSettings] = useState({
    format: 'png',
    quality: 1,
    scale: 1,
    withWatermark: false,
    watermarkText: 'SaintCanvas'
  });

  const handleExport = () => {
    if (!stageRef.current) return;

    const uri = stageRef.current.toDataURL({
      mimeType: `image/${settings.format === 'jpg' ? 'jpeg' : settings.format}`,
      quality: settings.quality,
      pixelRatio: settings.scale
    });

    // Download
    const link = document.createElement('a');
    link.download = `saint-canvas-${Date.now()}.${settings.format}`;
    link.href = uri;
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);

    onClose();
  };

  if (!isOpen) return null;

  return (
    <div className="modal-overlay">
      <div className="modal export-modal">
        <div className="modal-header">
          <h2>Xuất Hình Ảnh</h2>
          <button onClick={onClose} className="close-btn">×</button>
        </div>

        <div className="modal-content">
          {/* Format Selection */}
          <div className="form-group">
            <label>Định dạng:</label>
            <div className="format-options">
              {['png', 'jpg', 'webp'].map((format) => (
                <button
                  key={format}
                  className={`format-btn ${settings.format === format ? 'active' : ''}`}
                  onClick={() => setSettings({ ...settings, format })}
                >
                  {format.toUpperCase()}
                </button>
              ))}
            </div>
          </div>

          {/* Quality (for jpg) */}
          {settings.format === 'jpg' && (
            <div className="form-group">
              <label>Chất lượng: {Math.round(settings.quality * 100)}%</label>
              <input
                type="range"
                min="0.1"
                max="1"
                step="0.1"
                value={settings.quality}
                onChange={(e) => setSettings({
                  ...settings,
                  quality: parseFloat(e.target.value)
                })}
              />
            </div>
          )}

          {/* Scale */}
          <div className="form-group">
            <label>Kích thước:</label>
            <select
              value={settings.scale}
              onChange={(e) => setSettings({
                ...settings,
                scale: parseFloat(e.target.value)
              })}
            >
              <option value="0.5">50%</option>
              <option value="1">100%</option>
              <option value="2">200%</option>
              <option value="3">300%</option>
            </select>
          </div>

          {/* Watermark */}
          <div className="form-group checkbox-group">
            <label>
              <input
                type="checkbox"
                checked={settings.withWatermark}
                onChange={(e) => setSettings({
                  ...settings,
                  withWatermark: e.target.checked
                })}
              />
              Thêm watermark
            </label>
          </div>
        </div>

        <div className="modal-footer">
          <button onClick={onClose} className="btn-secondary">
            Hủy
          </button>
          <button onClick={handleExport} className="btn-primary">
            <i className="icon-download" />
            Tải Xuống
          </button>
        </div>
      </div>
    </div>
  );
};

export default ExportModal;
```

---

## 🎨 Giao Diện Chính

```jsx
// pages/EditorPage.jsx
import React, { useRef, useState } from 'react';
import Header from '../components/Layout/Header';
import Sidebar from '../components/Layout/Sidebar';
import EditorCanvas from '../components/Editor/EditorCanvas';
import ToolBar from '../components/Editor/ToolBar';
import LayerPanel from '../components/Editor/LayerPanel';
import AssetLibrary from '../components/Library/AssetLibrary';
import ExportModal from '../components/Export/ExportModal';
import { useEditorStore } from '../store/editorStore';

const EditorPage = () => {
  const stageRef = useRef(null);
  const [showExportModal, setShowExportModal] = useState(false);
  const [sidebarTab, setSidebarTab] = useState('library'); // 'library' | 'layers'
  const { baseImage } = useEditorStore();

  return (
    <div className="editor-page">
      <Header onExport={() => setShowExportModal(true)} />
      
      <div className="editor-layout">
        {/* Left Sidebar - Asset Library */}
        <aside className="left-sidebar">
          <AssetLibrary />
        </aside>

        {/* Main Editor Area */}
        <main className="editor-main">
          <ToolBar />
          
          <div className="canvas-container">
            {baseImage ? (
              <EditorCanvas ref={stageRef} />
            ) : (
              <div className="empty-canvas">
                <ImageUploader />
              </div>
            )}
          </div>
        </main>

        {/* Right Sidebar - Layers */}
        <aside className="right-sidebar">
          <div className="sidebar-tabs">
            <button
              className={sidebarTab === 'layers' ? 'active' : ''}
              onClick={() => setSidebarTab('layers')}
            >
              Các Lớp
            </button>
            <button
              className={sidebarTab === 'properties' ? 'active' : ''}
              onClick={() => setSidebarTab('properties')}
            >
              Thuộc Tính
            </button>
          </div>
          
          {sidebarTab === 'layers' && <LayerPanel />}
          {sidebarTab === 'properties' && <PropertiesPanel />}
        </aside>
      </div>

      <ExportModal
        isOpen={showExportModal}
        onClose={() => setShowExportModal(false)}
        stageRef={stageRef}
      />
    </div>
  );
};

export default EditorPage;
```

---

## 🛠️ Tech Stack Đề Xuất

```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.x",
    
    "react-konva": "^18.x",
    "konva": "^9.x",
    "use-image": "^1.x",
    
    "zustand": "^4.x",
    
    "react-dropzone": "^14.x",
    "react-beautiful-dnd": "^13.x",
    "react-tabs": "^6.x",
    
    "axios": "^1.x",
    "uuid": "^9.x",
    
    "framer-motion": "^10.x",
    
    "tailwindcss": "^3.x"
  },
  "devDependencies": {
    "vite": "^5.x",
    "@vitejs/plugin-react": "^4.x"
  }
}
```

---

## 📱 Responsive Design

```css
/* styles/editor.css */
.editor-page {
  display: flex;
  flex-direction: column;
  height: 100vh;
}

.editor-layout {
  display: grid;
  grid-template-columns: 280px 1fr 260px;
  flex: 1;
  overflow: hidden;
}

/* Tablet */
@media (max-width: 1024px) {
  .editor-layout {
    grid-template-columns: 1fr;
  }
  
  .left-sidebar,
  .right-sidebar {
    position: fixed;
    z-index: 100;
    height: 100%;
    transform: translateX(-100%);
    transition: transform 0.3s ease;
  }
  
  .left-sidebar.open,
  .right-sidebar.open {
    transform: translateX(0);
  }
  
  .right-sidebar {
    right: 0;
    transform: translateX(100%);
  }
}

/* Mobile */
@media (max-width: 768px) {
  .toolbar {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    flex-wrap: wrap;
    justify-content: center;
  }
}
```

---

## 🚀 Các Tính Năng Nâng Cao (Tương Lai)

### 1. **Tích hợp AI (Backend)**
```javascript
// services/aiService.js
export const aiService = {
  // Tự động xóa nền
  removeBackground: async (imageData) => {
    const response = await api.post('/ai/remove-bg', { image: imageData });
    return response.data.result;
  },
  
  // Gợi ý vị trí đặt object
  suggestPlacement: async (baseImage, objectType) => {
    const response = await api.post('/ai/suggest-placement', {
      image: baseImage,
      type: objectType
    });
    return response.data.suggestions;
  },
  
  // Tạo hình ảnh với prompt
  generateImage: async (prompt) => {
    const response = await api.post('/ai/generate', { prompt });
    return response.data.generatedImage;
  }
};
```

### 2. **Template System**
```javascript
// Các template có sẵn cho người dùng
const TEMPLATES = [
  {
    id: 'saint-card',
    name: 'Thiệp Thánh',
    thumbnail: '/templates/saint-card-thumb.jpg',
    layers: [
      { type: 'background', preset: 'golden-frame' },
      { type: 'placeholder', label: 'Hình Thánh' },
      { type: 'text', label: 'Tên Thánh' },
      { type: 'text', label: 'Lời Cầu Nguyện' }
    ]
  },
  {
    id: 'prayer-card',
    name: 'Thiệp Cầu Nguyện',
    // ...
  }
];
```

### 3. **Cloud Save & Share**
```javascript
// Lưu trữ và chia sẻ project
const projectService = {
  save: async (projectData) => { /* ... */ },
  load: async (projectId) => { /* ... */ },
  share: async (projectId, shareSettings) => { /* ... */ }
};
```

---

## 📊 User Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         TRANG CHỦ                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │ Bắt Đầu Mới │  │ Mở Project  │  │ Xem Gallery/Templates  │  │
│  └──────┬──────┘  └──────┬──────┘  └───────────┬─────────────┘  │
└─────────┼────────────────┼─────────────────────┼────────────────┘
          │                │                     │
          ▼                ▼                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                      TRANG EDITOR                               │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    TOOLBAR                               │   │
│  │  [Undo] [Redo] | [Transform] [Opacity] | [Export]       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌────────────┐  ┌─────────────────────────┐  ┌────────────┐   │
│  │  THƯ VIỆN  │  │                         │  │   LAYERS   │   │
│  │            │  │                         │  │            │   │
│  │ [Thánh]    │  │      CANVAS             │  │ Layer 3    │   │
│  │ [Trang Phục]│ │                         │  │ Layer 2    │   │
│  │ [Nền]      │  │    (Kéo thả, chỉnh sửa) │  │ Layer 1    │   │
│  │ [Đối tượng]│  │                         │  │ Base Image │   │
│  │            │  │                         │  │            │   │
│  └────────────┘  └─────────────────────────┘  └────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        XUẤT HÌNH ẢNH                            │
│  [PNG] [JPG] [WebP] | Chất lượng | Kích thước | [Tải Xuống]    │
└─────────────────────────────────────────────────────────────────┘
```

---

## ✅ Checklist Phát Triển

### Phase 1 - MVP
- [ ] Setup project với Vite + React
- [ ] Implement Upload Module
- [ ] Setup Canvas cơ bản với React-Konva
- [ ] Tạo cấu trúc dữ liệu assets
- [ ] Implement Drag & Drop assets lên canvas
- [ ] Basic layer management
- [ ] Export hình ảnh

### Phase 2 - Core Features
- [ ] Transform controls (resize, rotate)
- [ ] Layer panel với reorder
- [ ] Undo/Redo
- [ ] Opacity & blend modes
- [ ] Asset search & filter

### Phase 3 - Polish
- [ ] Responsive design
- [ ] Keyboard shortcuts
- [ ] Loading states
- [ ] Error handling
- [ ] Performance optimization

### Phase 4 - Advanced
- [ ] Template system
- [ ] Cloud save
- [ ] AI integration
- [ ] User accounts

---

Bạn muốn tôi đi sâu vào phần nào cụ thể không? Ví dụ:
1. Chi tiết hơn về Canvas manipulation
2. CSS/Styling chi tiết
3. Hệ thống assets management
4. API integration cho AI features